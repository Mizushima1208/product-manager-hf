version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/app/data/vision_credentials.json
    restart: unless-stopped
